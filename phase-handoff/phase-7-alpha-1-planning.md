# Phase 7-alpha-1-planning: Sprint α-1 計画立案 Handoff

## Phase Meta

| 項目 | 値 |
|---|---|
| Phase 境界 | Phase 6-poc-final (sealed, 16/16) → 本 Phase (計画立案 + adversarial-review + reconciliation) → Phase 8 (α-1 実装着手) |
| 状態 | sealed (計画完成、実装未着手) |
| 担当 | Claude (planner agent + spec 突合) + Codex (adversarial-review) |
| 成果物 | 3 ファイル (plan v1.1 / review / reconciliation) + Task #1-#16 (#13/#15/#16 完了) |
| dev server | 不使用 |
| commits | 0 (本セッションはドキュメントのみ、コミット未実施) |

## 本 Phase 達成事項

1. **planner agent** で Sprint α-1 計画 v1.0 策定 (Phase A→E + 依存グラフ + Codex 委任ポイント)
2. **Codex adversarial-review** で Critical 補正 5 件抽出 (`codex exec --sandbox read-only` 経由、末尾 50 行のみ取得)
3. **schema reconciliation** で 4 テーブル全列突合 (RENAME 6 / NOT NULL 多数 / Index 7 / pii_anonymization_jobs 新規 15 列)
4. **計画 v1.1 補正** で record_audit_log を Critical risk + Claude 単独実装に格上げ、pii state machine 同じく
5. **audit_logs_cleanup_log** を α-2 送り決定 (spec §11.3 line 1315 言及のみで本体未定義)

## 重要設計判断

1. **record_audit_log trigger は Claude 単独実装** (Codex 委任不可)
   - 理由: 1 defect で audited 全テーブル書込破壊。recursion 防止 (`pg_trigger_depth() > 1` 関数先頭) + per-table contract + 9 テーブル列挙必須
   - test matrix 27 assertion (9 テーブル × 3 action) を merge 前必須化
2. **pii_anonymization_jobs を state machine として Claude 単独実装** (Codex 委任不可)
   - 理由: spec §11.2b で 7 状態 + EXCLUDE constraint + retry_count + legal_hold_reason 既定義
3. **vendor_portal_inbox RLS は二重防衛**
   - `vendor_id = current_vendor_id() AND (recipient_vendor_user_id IS NULL OR = current_vendor_user_id())`
4. **schema RENAME 6 件を Codex 委任時に明示必須** (alpha-1-schema-reconciliation.md 参照)
5. **audit_logs_cleanup_log + v_accounting_audit_trail は α-2 送り** (spec 不完備 + 依存テーブル未完備)

## 次 Phase (Sprint α-1 実装) 入力契約

### 最初に読むべきファイル (順)
1. `CLAUDE.md` (日付禁止メタルール) — プロジェクトルート
2. `phase-handoff/phase-7-alpha-1-planning.md` (本ファイル)
3. `phase-handoff/sprint-alpha-1-plan.md` v1.1 (Phase 分割・Codex 委任境界)
4. `phase-handoff/alpha-1-schema-reconciliation.md` (4 テーブル全列突合)
5. `phase-handoff/alpha-1-plan-review.md` (Codex review、参考)
6. `spec/data-model.md` §3-§13, §17, §11.2b
7. `spec/CLAUDE.md` (実装規律)

### 最初の着手タスク (Task #1)
**Phase A-1: 21 DDL ファイル生成 (Codex 委任)** — sprint-alpha-1-plan.md v1.1 の Phase A セクション参照。Codex プロンプトに reconciliation.md の RENAME/NOT NULL/Index 全件を明示必須。

### 絶対に壊してはいけないもの
- `pit_v24_poc` schema 全体 (α-1 cleanup 前は残置)
- spec §7.10.2 関数仕様 (accept_invitation は B-1d で advisory lock 化)
- 前 Phase の不変ルール継承 (vendors 2 RLS / dotenv 2 段 / typedRoutes top-level)

### 残タスク (Phase A-1 〜 E-2)
- Task #1-#12 (Phase A-1〜A-3, B-1〜B-2, C-1〜C-3, D-1〜D-3, E-1)
- Task #14 (Phase 完了時 seal)
- 新規追加候補: Phase B-1 分解後の細分タスク + Phase C-2b record_audit_log Claude 単独実装 + Phase E-2 test matrix

## watchpoint 継承 (前 Phase から)

- Next.js HMR 404 (admin route group): 編集後 `.next` clean + dev restart
- agent-browser 不可: Playwright 直接利用
- **Codex Windows sandbox 制約**: `codex exec` 直叩きでも file write 不可、`--sandbox read-only` でも spawn 失敗あり (Codex MCP 経由 Task → fallback to `codex exec` 推奨)
- Codex 出力を Bash パイプで `tail` すると先頭欠落 → 全文取得は redirect to file 推奨
- mcp__supabase__create_branch: project_ref permission 不可、schema 隔離 fallback 運用
- DATABASE_URL :6543 ?pgbouncer=true + `prepare: false` 必須 (50 並列実績)、DIRECT_URL :5432 は順次 apply 用 (15 接続上限)

## Phase 振り返りメモ

- うまくいった: planner agent 出力 → Codex review → schema reconciliation の 3 段検証で Critical 5 件補正を計画反映前に確定
- うまくいった: phase-handoff/phase-6-poc-final.md の「次 Phase 入力契約」が即座に α-1 計画立案の起点になった
- 改善余地: Codex CLI 直叩き時に `tail -50` で先頭 (Verdict + A.1) 欠落 → 次回 redirect to file 必須
- 改善余地: Codex Windows sandbox spawn 失敗は依然解決せず、`--sandbox read-only` でも file write 不可。Codex 出力中継は Claude 経由パターンを確立 (本セッションで実証)

---

*Generated by phase-handoff skill (seal mode) — Sprint α-1 計画立案完了、実装未着手*
