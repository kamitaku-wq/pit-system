# Phase 8-alpha-1-A: Sprint α-1 Phase A (DDL 基盤) Handoff

## Phase Meta

| 項目 | 値 |
|---|---|
| Phase 境界 | Phase 7 sealed (α-1 計画) → 本 Phase (α-1 Phase A 実装) → Phase 9 (α-1 Phase B 実装) |
| 状態 | sealed (Phase A 全 DoD 達成、DDL apply 完了) |
| 担当 | Claude (計画・review・引き取り修正) + Codex (実装) |
| 関連 commits | `388f8ed` (A-1) / `f6a7fe3` (A-2) / DB apply (A-3, git 外) |
| dev server | 不使用 |

## 本 Phase 達成事項

1. **A-1**: alpha-1-public 配下 21 DDL ファイル生成 (Codex 委任、commit 388f8ed)
2. **A-1**: reconciliation 全件反映 (RENAME 6 / NOT NULL / Index 7 / α-2 送り除外)
3. **A-2**: Drizzle schema 43 新規 + 3 更新 + index.ts (Codex 委任、commit f6a7fe3)
4. **A-2**: Claude 引き取りで users.ts SQL/Drizzle 乖離修正 (roleId nullable, defaultStoreId FK 削除)
5. **A-3**: 既存 public 4 テーブル + 2 slice test DROP CASCADE → 21 SQL 全 apply (0 skipped)
6. **A-3**: DoD `count(public.business_tables) = 46` 達成 + matview 3 件
7. **seed master**: lane_types 6 / statuses 16 / roles 6 / status_transitions 19 / notification_rules 6 挿入確認

## 重要設計判断

1. **SQL を真実の源**: A-2 で Drizzle/SQL 乖離 (roleId NOT NULL vs nullable, defaultStoreId FK) 検出 → Drizzle 側修正で SQL に合わせる。spec 厳格化は別途
2. **18/19/20 stub**: helper/RLS/triggers は Phase B-1a/C-1/C-2a で本実装、A-1 では 1 行コメントのみ
3. **既存 public 4 テーブル DROP**: rows=0 で破壊リスク低、CREATE TABLE 衝突回避、`_raw_migrations` は残す
4. **α-2 送り厳守**: pii_anonymization_jobs (B-1b) / audit_logs_cleanup_log / v_accounting_audit_trail VIEW を SQL/Drizzle 双方除外
5. **Codex sandbox 引き取り**: A-2 で typecheck/git 等 pwsh spawn error 多発 → cancel + Claude 実行で完走

## 次 Phase (Sprint α-1 Phase B) 入力契約

### 最初に読むべきファイル (順)
1. `phase-handoff/phase-8-alpha-1-A.md` (本ファイル)
2. `phase-handoff/sprint-alpha-1-plan.md` v1.1 (Phase B 詳細)
3. `phase-handoff/alpha-1-schema-reconciliation.md` (Table 3 pii_anonymization_jobs 仕様)
4. `src/lib/db/raw-migrations/alpha-1-public/18_helper_functions.sql` (stub 状態確認)
5. `src/lib/db/raw-migrations/poc-12-schema-isolation/poc12_18_helper_functions.sql` (helper 5 関数移植元)
6. `src/lib/db/raw-migrations/poc-16-pii-redaction/poc16_01_function.sql` (redact_audit_payload 移植元)
7. `spec/data-model.md` §11.2b (pii_anonymization_jobs state machine) / §14.2 (helper) / §7.10.2 (accept_invitation)

### 最初の着手タスク
**Phase B-1a (Codex 委任): 5 helper 関数を 18_helper_functions.sql に実装**
- current_user_company_id / current_vendor_id / current_vendor_user_id / vendor_accessible_company_ids / vendor_invited_transport_order_ids

### 並走可能
- **B-1b** (Claude 単独): pii_anonymization_jobs テーブル + state machine (B-1a と独立)
- **B-1c** (Claude 単独): redact_audit_payload 移植 (B-1a と独立)
- **B-1d** (Claude 単独): accept_invitation advisory lock 化 (B-1a 完了後)

### 絶対に壊してはいけないもの (invariants)
- `_raw_migrations` テーブル (apply tracking、削除すると再 apply 強制)
- `pit_v24_poc` schema 全体 (α-2 cleanup 前は残置、PoC 履歴)
- 46 業務テーブルの構造 (apply 済、Phase B/C は関数・RLS・trigger 追加のみ)
- `REVOKE UPDATE, DELETE ON audit_logs FROM authenticated, anon` (append-only 保持)
- Drizzle schema 47 ファイル (SQL と整合済)

## watchpoint 継承

- **Codex Windows sandbox spawn error**: pwsh.exe からの pnpm/git/Get-Date 等が exit -1 で失敗。typecheck/commit は Claude が引き取る pattern を予め組み込む
- Codex 出力中継: マーカー方式 (=== FILE: ... ===) は Phase A-1 で有効、Phase A-2 では Codex 直接 Write 経路も成功
- DIRECT_URL :5432 + `prepare:false` で raw SQL 21 件 apply 完走 (15 接続上限以下)
- mcp__supabase__execute_sql で DDL/集計クエリ可、create_branch は依然不可
- audit_logs は updated_at 列なし (append-only)、record_audit_log trigger 設計時に updated_at 触らない

## Phase 振り返りメモ

- **うまくいった**: A-1 マーカー方式 + Claude Write 経路、reconciliation 全件反映 sanity check、SQL/Drizzle 乖離検出と即修正
- **うまくいった**: A-3 DROP CASCADE → apply の流れが clean (rows=0 でリスク低)、DoD 一発達成
- **改善余地**: A-2 で Codex が無限 retry に陥った (16m45s, sandbox spawn error 多発) → 介入閾値 (5m or 10 spawn errors) を次回設定し早期 cancel + 引き取り
- **改善余地**: Codex 委任プロンプトで「users.ts の roleId を NOT NULL + FK」と書いてしまい SQL と乖離 → 委任前に SQL 側真実の源確認を必須化

## Codex ledger refs

- `del-20260523-141946-6aa7` (A-1, applied, 21 DDL)
- `del-20260523-143546-152f` (A-2, applied + Claude 引き取り修正, 47 Drizzle files)

## 主要メトリクス

| 指標 | 値 |
|---|---|
| 追加 commit 数 | 2 (388f8ed, f6a7fe3) |
| 追加コード行数 | ~819 SQL + ~1300 TS = ~2100 行 |
| 公開 public.business_tables | 46 (DoD ✅) |
| matviews | 3 |
| Codex 委任率 | ~95% (Claude は乖離修正 + apply + DoD 検証のみ) |
| セッション数 | 1 (本セッション、次は /clear 後) |

---

*Generated by phase-handoff skill (seal mode) — Phase A 完了、Phase B 着手準備済*
