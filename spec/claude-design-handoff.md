# 段取りくん — Claude Design ハンドオフ資料 v2.2

> 作成日: 2026-05-22（v2.1）/ 2026-05-23（v2.2 §A.8.11 追加）
> 目的: Claude Design に本番品質の UI を作ってもらうための完全要件書。
> 関連: `screen-list.md` v2.2（画面一覧）/ `requirements.md` v2.2（業務ロジック）/ `data-model.md` v2.2（データ構造）
> **v2.1 で追加**: §A.8 用語ポリシー（DB スキーマ列名・英語スネークケース・業界略語を UI に出さないルール）
> **v2.2 で追加**: §A.8.11「顧客に見せない概念」— マルチテナント / SaaS / 他社 / 共有という存在を顧客に開示しないポリシー

## このドキュメントの使い方

このファイルは Anthropic の **Claude Design**（Research Preview）に渡し、プロトタイプを生成するための完全な仕様書です。

### Claude Design に伝える流れ
1. このファイルを丸ごとプロンプトに貼る
2. 「§A ブランドガイドラインに従い、§B 共通コンポーネント / §C-G 各画面群を順にプロトタイプ化してください」と指示
3. 生成された画面を確認、ハンドオフバンドル（コンポーネント・トークン・コピー）を取得
4. ハンドオフバンドルを営業資料（13 ページ）に組み込む

---

# §A. ブランドガイドライン

## A.1 プロダクト概要

| 項目 | 内容 |
|---|---|
| サービス名 | **段取りくん** |
| 英字表記 | **Dandori-kun**（必要時） |
| タグライン | 面倒な段取りを、段取りくんが全部やってくれる。 |
| 領域 | 中古車販売会社向け BtoB 業務クラウド：整備ピット予約 + 店間整備 + 業者通知 |
| 対象ユーザー | 多店舗展開する中古車販売会社の経営層・現場スタッフ・回送業者・顧客 |

## A.2 トーン

- **モダン・軽快な業務システム調**（Notion / Linear / Vercel 系の現代的フラットデザイン）
- **白基調 + 余白多め**
- **名前は親しみやすく、UI は信頼感重視**のバランス
- マスコット・キャラクターは使わない（or 最小限のロゴマーク程度）

## A.3 カラーパレット

| 用途 | 色 | HEX | 用法 |
|---|---|---|---|
| **Primary（メインアクセント）** | 濃紺 | `#1E3A8A` | CTA ボタン、リンク、見出し強調、選択中タブ |
| Primary Hover | | `#1E40AF` | hover/active 状態 |
| Primary Light | | `#DBEAFE` | バッジ背景、選択中行のハイライト |
| **Success** | 緑 | `#10B981` | 完了、対応可、成功通知 |
| Success Light | | `#D1FAE5` | 成功バッジ背景 |
| **Warning** | 黄 | `#F59E0B` | 業者未確認、対応待ち |
| Warning Light | | `#FEF3C7` | 警告バッジ背景 |
| **Danger** | 赤 | `#EF4444` | 対応不可、通知失敗、エラー |
| Danger Light | | `#FEE2E2` | エラーバッジ背景 |
| **Neutral（テキスト）** | 黒系 | `#0F172A` | 本文 |
| Text Secondary | | `#475569` | サブテキスト |
| Text Muted | | `#94A3B8` | プレースホルダ、補助 |
| **Background** | 白 | `#FFFFFF` | メイン背景 |
| BG Subtle | | `#F8FAFC` | サイドバー、セクション背景 |
| BG Card | | `#FFFFFF` | カード |
| Border | | `#E2E8F0` | 境界線、divider |
| Border Strong | | `#CBD5E1` | フォーム枠 |

## A.4 タイポグラフィ

| 用途 | フォント | サイズ | 太さ |
|---|---|---|---|
| 見出し H1 | Inter / Noto Sans JP | 32px | 700 |
| 見出し H2 | Inter / Noto Sans JP | 24px | 700 |
| 見出し H3 | Inter / Noto Sans JP | 18px | 600 |
| 本文 | Noto Sans JP | 14px | 400 |
| 本文（強調） | Noto Sans JP | 14px | 600 |
| キャプション | Noto Sans JP | 12px | 400 |
| 数字（KPI 強調） | Inter | 48px / 56px | 700 (tabular-nums) |
| ボタン | Inter / Noto Sans JP | 14px | 500 |

## A.5 スペーシング

8px グリッドベース。`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`px を基本とする。

- セクション間: 48px
- カード内 padding: 24px
- フォーム要素間: 16px
- ボタン padding: 12px 24px

## A.6 角丸・影

- Border radius: `4px`（小要素）/ `8px`（カード）/ `12px`（モーダル）
- Shadow: `0 1px 2px rgba(15, 23, 42, 0.06)`（カード）/ `0 10px 25px rgba(15, 23, 42, 0.10)`（モーダル）

## A.7 アイコン

- **Lucide Icons** を採用（線画、22px / 16px、stroke 1.5px）
- ステータスバッジ用の専用アイコン:
  - 対応可 → CheckCircle2
  - 対応不可 → XCircle
  - 業者未確認 → Clock
  - 通知失敗 → AlertTriangle
  - 移動中 → Truck
  - 完了 → Flag

## A.8 用語ポリシー（v2.1 で追加・最重要）

想定ユーザーは **経営層・現場スタッフ・回送業者・顧客（高齢者含む）** であり、いずれも IT エンジニアではない。以下のルールを **すべての画面・ラベル・ヒント・エラーメッセージ・印刷物** に適用すること。

### A.8.1 UI に絶対に露出させてはいけないもの

| 種別 | 例 | 代わりに |
|---|---|---|
| **DB スキーマ列名** | `responsibility_disclaimer_text` / `companies.transport_disclaimer_text` / `is_shared` / `vendor_id` | 「免責文言」/ `is_shared` は **UI 露出禁止**（§A.8.11 参照）/ 「業者」 |
| **英語スネークケースの enum** | `tentative` / `confirmed` / `in_progress` / `done` / `cancelled` | 「仮予約」「確定」「作業中」「完了」「キャンセル」 |
| **DB 関係性用語** | `M2M`（多対多） / `FK`（外部キー） / `enum` / `entity` | 「対応できる作業メニュー」「対象別」 |
| **業界略語** | `KYC` / `i18n` / `OCR` / `SLA` | 「本人確認」「テンプレート名」「文字認識」「対応時間」 |
| **プログラミング用語** | `サイドエフェクト` / `条件式` / `トリガー`（単独） / `バリデーション` | 「同時に実行されること」「条件」「きっかけ」「入力チェック」 |
| **管理画面文化用語** | `マスター` / `マトリクス` / `ログ` / `inbox` | 「一覧」or 「登録〜」/「対応表」/「記録」or 「履歴」/「通知一覧」 |

### A.8.2 ラベルとヒントの責任分担

- **ラベル**：ユーザー向けの自然な日本語短文
- **ヒント（hint）**：補足説明。動作の効果・記録される副作用・単位などを記述
- **内部識別子（コード、URL、テーブル名）**：絶対に表示しない。`data-*` 属性 / `aria-describedby` で開発者向けにのみ保持

例:

```jsx
// NG
<Field label="responsibility_disclaimer_text"
       hint="companies.transport_disclaimer_text · 改訂版は監査ログに残ります">

// OK
<Field label="免責文言"
       hint="回送依頼書 PDF に印字されます。変更内容は操作記録に残ります">
```

### A.8.3 ステータスの表記統一

`statuses` テーブルの `key`（英語スネークケース）と `name`（日本語）を **画面では `name` のみ使用**。状態遷移エディタ（F.13）でもノードラベルは日本語にする。デバッグ用に key を見せる場合は薄文字の補助表示に限定。

### A.8.4 ナビゲーションラベル（決定版）

| キー | 表示ラベル | NG |
|---|---|---|
| `audit` | 操作記録 | 監査ログ |
| `ops` | 通知の再送・確認 | 通知失敗運用 |
| `vendor-master` | 業者一覧 | 業者マスター |
| `vendor-users` | 業者ログインアカウント | 業者ユーザー |
| `vendor-inbox` | 通知一覧 / お知らせ | 通知 inbox |
| `state-transitions` | 状態の進み方ルール | 状態遷移ルール |
| `permissions` | 権限と役割 | 権限マトリクス |

### A.8.5 設定画面の用語対応表（経営層向け）

| 内部用語 / IT 用語 | UI 表示 |
|---|---|
| エンティティ | 対象 / 種類 |
| トリガー | きっかけ / 開始条件 |
| サイドエフェクト | 同時に実行されること |
| バッファ | 準備時間 / 余裕時間 |
| クールダウン | 再送までの待ち時間 |
| 実行可能ロール | 実行できる役割 |
| ロール | 役割 |
| マトリクス | 対応表 / 一覧 |
| マスター | 一覧 / 登録 〜 |
| カラム / フィールド | 項目 |
| バリデーション | 入力チェック |
| デフォルト | 初期値 |
| エクスポート | 書き出し / ダウンロード |
| インポート | 読み込み |

### A.8.6 例外ルール

- **業務用語（業界共通）はそのまま使う**：「整備伝票」「車検整備」「回送」「陸送」「VIN（車台番号）」「型式」など中古車・整備業界の専門用語は使用可
- **業務番号**：`TY-2026-0142`（回送）`SV-2026-0089`（整備伝票）`R-2026-0042`（予約）`KEY-0142`（鍵）は **業務上の識別子なので使用可**
- **法的表記**：「業務委託契約書」「責任分界」「免責」「同意」「個人情報保護法」「特定商取引法に基づく表記」など法務文言はそのまま

### A.8.7 用語チェックの自動化（推奨）

実装時に lint ルール / pre-commit hook で以下を検出：

```
禁止パターン:
  - DB 列名: /\b[a-z_]+_[a-z_]+(?:_[a-z]+)*\b/ が `label=` / `placeholder=` / `>...<` 内に出現
  - 業界略語: KYC / M2M / i18n / FK / enum
  - 半英語: tentative / confirmed / pending / accepted / rejected / winning_bid / revoked が UI 表示部分に出現
  - 略号: R / W / C（Read/Write/Create）を権限マトリクスのセル内表示に使用
  - 英語ヘッダ: WHEN / TO / Email / In-app / SMS / Slack を表ヘッダに使用
```

### A.8.8 個人情報モックの厳格ルール（Codex 指摘で追加）

§L 補足。**プレースホルダ風表記の徹底**：

| ✗ NG | ✓ OK |
|---|---|
| `tanaka@example.com` | `t****@example.com` |
| `田中 様 tanaka@example.com` | `田中 様 t****@example.com` |
| `090-1234-5678` | `090-xxxx-1234`（末尾 4 桁のみ可、または完全マスク `090-xxxx-xxxx`） |
| `ABCD1234567890ABC` | `***ABC` または `ABCD-12-3456789` の途中省略 |

中古車屋らしさを出すために姓名（田中 太郎 / 佐藤 花子）は使ってよいが、**個人を特定可能な実情報（実電話・実メール）に見えるパターンは禁止**。

### A.8.9 ARIA 属性とフォーム ID 連携（必須、Codex 指摘）

すべてのフォームコンポーネントは：

```jsx
// NG（現状）
<Field label="店舗"><input className="input" /></Field>

// OK（必須対応）
const fieldId = useId();
<Field label="店舗" id={fieldId} hint="..." required>
  <input
    id={fieldId}
    aria-describedby={`${fieldId}-hint`}
    aria-required={required}
    aria-invalid={hasError}
    className="input"
  />
</Field>
```

スクリーンリーダー対応 + キーボードナビゲーション必須。

### A.8.10 モバイル写真添付（必須、Codex 指摘）

業者ポータルの写真添付ボタンは **HTML 仕様で `capture` 属性を必ず付与**：

```jsx
<input
  type="file"
  accept="image/*"
  capture="environment"  // 背面カメラ起動
  aria-label="現場の写真を撮影"
/>
```

業者は外出先のスマホ運用が前提のため、ファイル選択ではなくカメラが即座に立ち上がる UX が必須。外観 / 内装 / メーターの **撮影チ�ェックリスト UI** を併用すると業務適合性が上がる。

### A.8.11 顧客に見せない概念（営業・UI 共通の最重要ポリシー、v2.1 追加）

**BtoB SaaS の鉄則として、本システムが「他社にも提供される共通サービス」であることを顧客に開示しない**。営業資料・UI・印刷物のすべてで「貴社専用システム」として見せる。

#### A.8.11.1 顧客向けに使用禁止の語彙

| 禁止語 | 理由 | 代わりに |
|---|---|---|
| **マルチテナント / テナント** | SaaS 設計用語、顧客に他社存在を示唆 | （言及不要） |
| **SaaS** | 「他社にも売っている」と思わせる | 「クラウドシステム」「業務システム」 |
| **販売会社単位 / 会社単位** | テナント概念の言い換え | （言及不要） |
| **他社 / 他販売会社** | 直接的に他社存在を示唆 | （言及不要） |
| **共有 / 共通業者**（テナント横断の意味で） | 他社存在を示唆 | （UI から完全削除、§A.8.11.3 参照） |
| **テナント境界 / 分離** | 他社存在を前提とした表現 | 「貴社専用クラウド領域」「貴社データの保護」 |
| **会社 ID / company_id** | 内部識別子 | （UI 露出禁止） |
| `is_shared` | DB 列名 + 他社概念 | **UI 完全非表示**、内部フラグ |

#### A.8.11.2 営業資料・UI での代替表現

| ❌ NG（他社存在を示唆） | ✅ OK（貴社専用として見せる） |
|---|---|
| 「販売会社単位の SaaS で他社のデータは見えません」 | 「貴社専用クラウド領域でデータを安全に保管します」 |
| 「マルチテナント分離設計」 | 「業界標準のクラウド分離設計」「ISO27001 準拠の保護」 |
| 「他社と共有する業者」（業者マスターのバッジ） | （UI から削除、複数拠点対応はフランチャイズ機能として別途設計） |
| 「貴社↔他社」の図解 | 「貴社専用クラウド」+ セキュリティ認証アイコン |
| 「全テナント共通の標準ステータス」 | 「初期搭載のステータス」「標準テンプレート」 |

#### A.8.11.3 業者マスター `is_shared` の UI 取り扱い

DB 内部では `vendors.is_shared` フラグを保持し続けるが、**UI からは完全に隠す**：

- 業者一覧の列「他社共有」「共通業者」**削除**
- バッジ表示「他社共有」「全社共通」**削除**
- フィルタチップ「共通業者を含む」**削除**
- 業者編集モーダルの「is_shared / 他社共有」スイッチ **削除**
- F.0 設定トップの業者マスターサブテキストから「他社共有」「共有設定」を削除し、「登録業者、対応エリア、対応店舗」のみに

将来、グループ会社・フランチャイズ加盟店向けに「グループ内共有業者」機能が必要になった場合は、別概念（`company_groups` テーブル等）として導入し、その際も顧客向け表現は **「貴社グループ内」「貴社の関連会社」** に限定する。

#### A.8.11.4 設定画面のデータ分離説明

F.0 設定トップに「データ分離インフォカード」**を置かない**。代わりに：

- **置く場合**：別途「セキュリティ・プライバシー」サブメニュー（F.18 新規）で、「貴社専用のクラウド領域」「アクセス制御」「監査ログ」「個人情報の保管期間」を **貴社目線のみ** で説明
- **置かない場合**：営業資料のセキュリティページ（p.11）でカバー、システム内には触れない

#### A.8.11.5 営業資料での扱い

営業資料 13 ページの **p.11 セキュリティ・データ保護** ページ：

| ❌ 削除 | ✅ 残す / 追加 |
|---|---|
| 「マルチテナント分離の見える化」 | 「貴社専用クラウド領域」 |
| 「他社からは構造的に見えない」 | 「外部からのアクセス遮断、操作はすべて記録」 |
| 「販売会社単位の SaaS」 | 「貴社の運用に合わせた専用システム」 |
| 「テナント境界」 | （言及なし） |

セキュリティ訴求は ISO27001 / SOC2 / 個人情報保護法 など **業界標準・法令準拠** の語彙で構成する。

#### A.8.11.6 内部設計文書との関係

- `requirements.md` / `data-model.md` / `implementation-plan.md` 等の **エンジニア向け内部設計文書では `tenant` / `company_id` / `SaaS` / `multi-tenant` 表現を維持**
- これらは顧客に提出しない前提
- 営業資料 / UI / 印刷物 / 業者ポータル / 顧客予約画面 / ヘルプ / FAQ などの **顧客接点物** からのみ削除

#### A.8.11.7 自動チェック追加（§A.8.7 補強）

```
禁止語の追加：
  - 顧客向けファイル（screen-*.jsx / 印刷 / 営業資料 / FAQ）に：
    - tenant / multi-tenant / マルチテナント / テナント
    - SaaS / Software as a Service
    - 他社 / 他販売会社 / 他テナント
    - is_shared / 全社共通 / 他社共有 / 全テナント共通
    - 販売会社単位 / 会社単位の SaaS
    - 「貴社↔他社」相当の図解
```

---

# §B. 共通コンポーネント

## B.1 グローバルヘッダ（全ページ共通）

```
┌─────────────────────────────────────────────────────────────┐
│ [段取りくん]  会社名 ▾ |  検索...      [🔔 3] [⚙] [👤 田中]  │
└─────────────────────────────────────────────────────────────┘
```

- 高さ 56px、白背景、下境界線
- 左：ロゴ「段取りくん」（濃紺）
- 中央左：会社名 + ドロップダウン（複数会社所属時切替、本部管理者のみ）
- 中央：検索バー（global search、車両 / 整備伝票 / 顧客）
- 右：通知ベル（未読バッジ赤）/ 設定アイコン / ユーザーメニュー

## B.2 サイドバーナビ（管理画面）

```
┌──────────────┐
│ ホーム         │
│ カレンダー     │
│ 顧客予約       │
│ 店間整備依頼   │  ← 新規依頼バッジ
│ 業者通知・回送 │  ← 未確認バッジ
│ 整備伝票       │
│ 車両一覧       │
│ ─────────    │
│ 通知失敗運用   │  ← 失敗件数バッジ
│ 監査ログ       │
│ ─────────    │
│ 設定           │
└──────────────┘
```

- 幅 240px、`#F8FAFC` 背景
- アクティブ項目：濃紺左ボーダー 3px + `#DBEAFE` 背景
- バッジ：右端、赤丸 + 数字

## B.3 サイドバーナビ（業者ポータル）

```
┌──────────────┐
│ 通知 inbox   │  ← 未読バッジ
│ 新規依頼       │
│ 対応中         │
│ 完了済み       │
│ ─────────    │
│ 招待管理       │
└──────────────┘
```

## B.4 ボタン

| 種類 | 用途 | スタイル |
|---|---|---|
| Primary | 確定、保存、送信 | 濃紺背景 + 白文字 |
| Secondary | キャンセル、戻る | 白背景 + 濃紺枠 + 濃紺文字 |
| Tertiary | リンク的アクション | テキストのみ（濃紺） |
| Danger | 削除、キャンセル予約 | 赤背景 + 白文字 |
| Ghost | ツールバー内 | 透明 + hover で薄グレー |

サイズ: lg (48px) / md (40px) / sm (32px)

## B.5 フォーム要素

- Input: 40px 高さ、border radius 4px、focus 時に濃紺リング 3px
- Select: Combobox 風、検索可能（react-select / cmdk）
- Date / Time picker: ポップオーバー型
- Textarea: 縦リサイズ可
- Checkbox / Radio: カスタムスタイル、濃紺チェック

## B.6 テーブル

```
┌──────────────────────────────────────────────────────┐
│ □ │ 列1     │ 列2 ▾  │ 列3        │ 列4     │ アクション │
├──┼─────────┼────────┼────────────┼─────────┼──────────┤
│ □ │ ...     │ ...    │ [バッジ]   │ ...     │ [⋮]      │
└──────────────────────────────────────────────────────┘
```

- ヘッダ：薄グレー背景、太字、ソート矢印
- 行：ホバーで `#F8FAFC` 背景、選択中はチェックボックス + 薄濃紺
- 行 padding: 12px 16px
- アクションは右端の縦三点ドット（メニュー展開）

## B.7 ステータスバッジ

色 + アイコン + テキストの組み合わせ:

```
[●対応可]     ← 緑背景 + チェックアイコン
[●未確認]    ← 黄背景 + 時計アイコン
[●対応不可]  ← 赤背景 + ×アイコン
[●移動中]    ← 青背景 + トラックアイコン
[●完了]      ← グレー背景 + フラグアイコン
```

## B.8 モーダル / ダイアログ

- 中央配置、最大幅 600px（大は 800px）
- オーバーレイ: `rgba(15, 23, 42, 0.5)`
- 角丸 12px、shadow lg
- 右上に閉じる×ボタン
- footer に Secondary（左）+ Primary（右）の 2 ボタン

## B.9 通知トースト

- 右下から出現、4 秒で自動消滅
- 成功（緑）/ 警告（黄）/ エラー（赤）/ 情報（青）

## B.10 楽観排他コンフリクト UI（v2.2 重要）

```
┌────────────────────────────────────────┐
│ ⚠ 他のスタッフが先に更新しました         │
├────────────────────────────────────────┤
│ あなたが編集していた予約は、田中さんが    │
│ 直前に更新しました。最新内容を確認して   │
│ から再操作してください。                 │
│                                        │
│ [変更内容を確認]  [再読込して再操作]    │
└────────────────────────────────────────┘
```

OptimisticLockError 検出時に表示するモーダル。

## B.11 通知 inbox エントリ（業者ポータル B.3）

```
┌────────────────────────────────────────┐
│ [●action_required] 新規回送依頼          │
│ ◯◯店 → ◯◯整備工場、TY-2026-0142     │
│ 5 分前                                 │
│ [既読化] [アーカイブ]                  │
└────────────────────────────────────────┘
```

- 未読は左ボーダー濃紺 3px + 太字
- severity アイコン: info / action_required(黄) / urgent(赤)

---

# §C. 管理画面（社内ユーザー向け、9 画面）

## C.1 ダッシュボード（screen-list.md §1.1）

### 目的
本日の整備状況と要対応タスクを 1 画面で把握。経営層・店長が朝一で開く想定。

### レイアウト

```
┌─────────────────────────────────────────────────────────────────┐
│ ホーム                                          [今日 2026-05-22] │
├─────────────────────────────────────────────────────────────────┤
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                   │
│ │本日予約 │ │業者未確認│ │対応不可 │ │通知失敗 │                   │
│ │  24件   │ │  3件 ●  │ │  1件 ● │ │  2件 ●  │                   │
│ └────────┘ └────────┘ └────────┘ └────────┘                   │
│                                                                 │
│ ┌─────────────────────────────┐ ┌─────────────────────────┐  │
│ │ 店舗別ピット稼働状況            │ │ 仮予約期限切れ間近        │  │
│ │ ▓▓▓▓▓▓░░ 渋谷店 75%         │ │ 14:30 田中様 オイル交換   │  │
│ │ ▓▓▓▓░░░░ 横浜店 50%         │ │ 15:00 佐藤様 タイヤ交換   │  │
│ │ ▓▓▓▓▓▓▓░ 川崎店 87%         │ │ ...                      │  │
│ └─────────────────────────────┘ └─────────────────────────┘  │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐  │
│ │ 本日のタイムライン（カレンダー縮小表示）                  │  │
│ │ ────────────────────────────                            │  │
│ └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 主要要素
- **KPI カード 4 つ**：本日予約 / 業者未確認 / 対応不可 / 通知失敗（バッジ赤）。クリックで該当一覧へ
- **店舗別ピット稼働状況**：横棒グラフ、店舗ごと、% 表示
- **仮予約期限切れ間近**：時系列リスト、クリックで予約詳細
- **本日のタイムライン**：簡易カレンダー（クリックで C.2 へ）

### 状態
- データなし：「本日の予約はまだありません」イラスト + プライマリボタン「新規予約」
- ロード中：スケルトン UI

---

## C.2 ピット予約カレンダー（screen-list.md §1.2）

### 目的
店舗・レーン・日時を視覚的に管理。予約作成・編集の起点。

### レイアウト

```
┌─────────────────────────────────────────────────────────────────┐
│ ピット予約カレンダー                  [+ 新規予約]                │
│ [日|週|月] [< 2026-05-22 >] [店舗: 全店 ▾] [レーン: 全 ▾]      │
├─────────────────────────────────────────────────────────────────┤
│        9:00  10:00 11:00 12:00 13:00 14:00 15:00 16:00 17:00  │
│ 渋谷   ┌─────┐                ┌──────────────┐                │
│ Lane1  │田中 │                │佐藤様 車検    │                │
│        │オイル│                │              │                │
│        └─────┘                └──────────────┘                │
│ 渋谷                                                            │
│ Lane2          ┌─────────┐                                     │
│                │鈴木様   │                                     │
│                │タイヤ   │                                     │
│                └─────────┘                                     │
│ 横浜   ┌─────────────┐  ●店間移動                              │
│ Lane1  │店間整備：田中│  ●業者: 〇〇運送                       │
│        │【業者通知済】│                                        │
│        └─────────────┘                                        │
└─────────────────────────────────────────────────────────────────┘
```

### 主要要素
- **ビュー切替タブ**：日 / 週 / 月（月は Phase 3 末）
- **日付ナビ**：前後矢印 + Today ボタン
- **フィルタ**：店舗 / レーン / 作業種別（複数選択可）
- **タイムグリッド**：横軸＝時間、縦軸＝レーン
- **予約ブロック**：
  - 顧客名 + 作業 + ステータスバッジ
  - 店間移動ありは右上に Truck アイコン
  - 業者手配状況をフッタに薄文字
  - クリックで詳細モーダル、ドラッグで時間変更
- **空き枠**：薄グレー、クリックで新規予約モーダル
- **競合**：is_double_booking はオレンジ斜線パターン

### 状態
- 営業時間外：濃グレー塗りつぶし
- 定休日：休業バッジ表示
- 同時編集競合：B.10 モーダル

---

## C.3 顧客予約一覧（screen-list.md §1.3）

### 目的
顧客来店予約を一覧管理、検索 / 変更 / キャンセル。

### レイアウト

```
┌─────────────────────────────────────────────────────────────────┐
│ 顧客予約一覧                                                     │
│ [検索: 顧客名/電話/ナンバー] [ステータス: 全 ▾] [期間 ▾] [CSV] │
├─────────────────────────────────────────────────────────────────┤
│ □ │ 予約日時          │ 顧客名  │ 店舗   │ 作業    │ ステータス │
│ □ │ 05/22 10:00      │ 田中太郎│ 渋谷店 │ オイル  │[●確定]    │
│ □ │ 05/22 11:30      │ 佐藤花子│ 横浜店 │ 車検    │[●確定]    │
│ □ │ 05/23 09:00      │ 鈴木一郎│ 川崎店 │ タイヤ  │[●仮予約]  │
│ ...                                                             │
└─────────────────────────────────────────────────────────────────┘
```

### 主要要素
- 検索バー + フィルタ（ステータス / 期間 / 店舗）
- テーブル（B.6）
- 行クリックで詳細スライドオーバー
- CSV エクスポートボタン

---

## C.4 店間整備依頼（screen-list.md §1.4）

### 目的
他店舗のピットを使う整備依頼を作成、業者選択 + 通知まで一気通貫。**最重要画面**。

### レイアウト（3 ステップウィザード）

#### Step 1: 基本情報

```
┌─────────────────────────────────────────────────────────────────┐
│ 店間整備依頼                                       [1] 2  3      │
├─────────────────────────────────────────────────────────────────┤
│ 整備伝票番号: [既存検索 or 新規 ▾]                              │
│                                                                 │
│ ─ 車両情報 ─                                                   │
│ 車両管理番号: [        ]  車種: [        ]                     │
│ ナンバー:    [        ]  車台番号: [        ]                  │
│                                                                 │
│ ─ 顧客情報 ─                                                   │
│ 顧客名: [        ]  電話: [        ]                            │
│                                                                 │
│ ─ 作業内容 ─                                                   │
│ 作業カテゴリ: [▾]  作業メニュー: [▾]                            │
│ 作業内容詳細: [テキストエリア]                                  │
│                                                                 │
│ [キャンセル]                              [次へ →]              │
└─────────────────────────────────────────────────────────────────┘
```

#### Step 2: 日時・レーン選択

```
┌─────────────────────────────────────────────────────────────────┐
│ 店間整備依頼                                       1 [2] 3      │
├─────────────────────────────────────────────────────────────────┤
│ 受付店舗: 渋谷店         作業店舗: [▾]                          │
│ 標準作業時間: 90 分（自動）  バッファ: 15 分（自動）            │
│                                                                 │
│ ─ 空き枠検索 ─                                                  │
│ ┌────────────────────────────────────────┐                    │
│ │ 05/23 (火)                              │                    │
│ │  9:00  ✓使用可  ✓使用可  -予約済  ✓   │                    │
│ │ 10:00  ✓使用可  -予約済  ✓使用可  ✓   │                    │
│ │ ...                                     │                    │
│ │ ▶ 横浜店 Lane1 を 10:00-11:30 で選択   │                    │
│ └────────────────────────────────────────┘                    │
│                                                                 │
│ [← 戻る]                                  [次へ →]              │
└─────────────────────────────────────────────────────────────────┘
```

#### Step 3: 店間移動・業者選択

```
┌─────────────────────────────────────────────────────────────────┐
│ 店間整備依頼                                       1  2 [3]     │
├─────────────────────────────────────────────────────────────────┤
│ □ 店間移動あり                                                  │
│                                                                 │
│ ─ 移動パターン ─                                                │
│ ◉ 片道（引取→搬入）                                            │
│ ○ 往復（引取→搬入→返却）                                       │
│ ○ 引取のみ                                                      │
│ ○ 三点移動                                                      │
│                                                                 │
│ 引取店舗: 渋谷店  搬入店舗: 横浜店  返却先: -                  │
│ □ 走行可否（チェックで自走可、外すとレッカー必須）              │
│ 希望引取: [05/23 09:30 ▾]  希望搬入: [05/23 10:00 ▾]          │
│                                                                 │
│ ─ 業者選択モード ─                                              │
│ ◉ 単一業者指名                                                  │
│   業者: [▾ ○○運送（渋谷-横浜エリア対応）]                    │
│ ○ 複数業者一斉打診（先着受注）                                  │
│ ○ スポット業者招待（未登録業者にメール）                        │
│                                                                 │
│ ─ 確定モード ─                                                  │
│ ◉ 自動確定（業者「対応可」で自動確定）                          │
│ ○ 手動確定（店舗承認が必要）                                    │
│                                                                 │
│ [← 戻る]                              [予約確定 + 業者通知]    │
└─────────────────────────────────────────────────────────────────┘
```

### 主要要素
- **ウィザード形式**（3 ステップ、上部ステッパー）
- **移動パターンによる UI 動的切替**：選択パターンで店舗フィールドの表示 / 非表示
- **業者選択モード切替**：単一 / 一斉打診 / 未登録招待で UI が変わる
  - 未登録招待時は `invitee_email / invitee_name` 入力フォーム表示
- **確定ボタン押下時**：プログレスインジケータ + 成功時はトースト「業者へ通知しました」

### 状態
- バリデーションエラー：赤テキスト + フィールド枠赤
- 業者対応エリア外：警告アイコン + ツールチップ
- 通信失敗：リトライボタン付きエラーモーダル

---

## C.5 業者通知・回送管理（screen-list.md §1.5、最重要）

### 目的
発注した店間移動の業者対応状況を一覧管理、トラブル時の対応を取れる。

### レイアウト

```
┌─────────────────────────────────────────────────────────────────┐
│ 業者通知・回送管理                                               │
│ [ステータス: 全▾] [業者: 全▾] [期間▾] [緊急対応のみ: □]       │
├─────────────────────────────────────────────────────────────────┤
│ # │ 依頼番号 │ 業者     │ 移動経路    │ 通知送信 │ 状態       │
│ 1 │ TY-0142  │ ○○運送  │渋→横       │ 5分前    │[●対応可]   │
│ 2 │ TY-0141  │ □□急便  │渋→川       │ 12分前   │[●未確認]  │
│ 3 │ TY-0140  │ △△陸送  │横→渋       │ 25分前   │[●対応不可]│
│ ...                                                             │
└─────────────────────────────────────────────────────────────────┘
```

### 行クリック → 詳細スライドオーバー

```
┌──────────────────────────────────────────┐
│ 依頼 TY-0142                            ×│
├──────────────────────────────────────────┤
│ 業者: ○○運送                          │
│ 担当: 山田次郎  📞 090-xxxx-xxxx        │
│                                         │
│ 通知送信: 2026-05-22 13:45 ✓           │
│ 業者確認: 2026-05-22 13:50 ✓           │
│ 対応可否: ✓ 対応可                     │
│ 引取予定: 2026-05-23 09:30             │
│ 搬入予定: 2026-05-23 10:30             │
│                                         │
│ ─ アクション ─                          │
│ [業者変更] [日時変更] [対応不可時の再打診]│
│ [キャンセル]                            │
│                                         │
│ ─ 招待管理（一斉打診時） ─              │
│ ○○運送  [●winning_bid]                │
│ □□急便  [●revoked]                    │
│ △△陸送  [●revoked]                    │
│                                         │
│ ─ 通知履歴 ─                            │
│ 通知1 ✓ Resend / sent / 13:45         │
│ ...                                    │
└──────────────────────────────────────────┘
```

### 主要要素
- 一覧テーブル + 行クリックでスライドオーバー詳細
- 詳細内：基本情報 / 業者状況 / アクション / 招待管理 / 通知履歴
- **アクションボタン**：
  - 業者変更（→ 業者選択モーダル + 再通知）
  - 日時変更（→ 日時変更モーダル + 差分通知）
  - 対応不可時の再打診（→ 次候補業者選択）
  - キャンセル（→ 確認モーダル）

### 状態
- 業者未確認 + 経過時間長：行に黄バッジ「要確認」
- 業者対応不可：行に赤背景 + アクションボタン強調
- 引取予定 + 3 時間経過で未更新：⚠ アイコン + アラート

---

## C.6 整備伝票一覧（screen-list.md §1.6）

### 目的
過去・現在の整備伝票を検索 / フィルタ / 印刷。

### レイアウト
標準テーブル（B.6）+ 検索バー + CSV / 印刷ボタン。行クリックで詳細ページへ遷移。

### 詳細ページ要素
- 伝票情報（番号 / 受付店 / 作業店 / 担当）
- 車両情報 + **過去整備履歴ジャンプリンク**
- 顧客情報
- 作業内容 / 想定時間 / 実績
- 金額（見積 / 請求）
- ステータス履歴（タイムライン）
- 印刷ボタン → PDF 出力

---

## C.7 車両一覧（screen-list.md §1.7）

### レイアウト
標準テーブル。検索（VIN / ナンバー / 顧客名）。行クリックで車両詳細。

### 車両詳細ページ
- 基本情報（管理番号 / 車種 / 年式 / VIN）
- 現所有者 + 過去所有履歴（vehicle_ownerships タイムライン）
- 関連整備伝票一覧（時系列）

---

## C.8 通知失敗・運用画面（screen-list.md §1.8、v2.2 で強化）

### 目的
通知配送失敗を可視化し、運用担当者が手動再送・原因調査を行う。

### レイアウト

```
┌─────────────────────────────────────────────────────────────────┐
│ 通知失敗・運用                                                   │
│ [ステータス: failed/cancelled/未対応 ▾] [割当: 全▾]            │
│                                                                 │
│ ⚠ 連続失敗 5 件発生中（本部管理者へエスカレーション送信済み）   │
├─────────────────────────────────────────────────────────────────┤
│ # │ outbox_id │ 種別            │ 対象     │ 試行 │ 状態 │ 担当  │
│ 1 │ ...       │ 業者通知（初回）│ ○○運送 │ 5/5  │failed│ -    │
│ 2 │ ...       │ 顧客リマインド   │ 田中様   │ 5/5  │failed│ 山田 │
│ ...                                                             │
└─────────────────────────────────────────────────────────────────┘
```

### 行クリック → 詳細
- 失敗理由（last_error 表示、技術的詳細をエンジニア用にも、業務的に「メールアドレス誤り」と要約）
- attempts / max_attempts
- 関連エンティティ（transport_order / reservation）リンク
- 配送ログ（notification_deliveries 全試行）
- 手動再送ボタン
- ステータス変更（failed → cancelled で諦め）
- 担当者割当
- メモ欄

---

## C.9 監査ログ閲覧（screen-list.md §1.9、本部管理者のみ）

### レイアウト
標準テーブル + 詳細スライドオーバー。検索（actor / entity_type / 期間 / action）。

### 詳細スライドオーバー
- before_json / after_json の **差分ハイライト表示**（diff viewer）
- IP / User-Agent
- CSV エクスポート

---

# §D. 業者ポータル（vendor_users 認証、6 画面）

## D.1 通知 inbox（screen-list.md §4.0、v2.1 新規）

### 目的
業者宛通知の未読 / 既読管理。ログイン直後の起点。

### レイアウト

```
┌─────────────────────────────────────────┐
│ 通知 inbox                              │
│ [未読 5] [すべて] [アーカイブ済]        │
├─────────────────────────────────────────┤
│ ●[action_required] 新規回送依頼          │
│   渋谷店 → 横浜整備工場                  │
│   TY-2026-0142 / 5 分前                 │
│   [既読化] [アーカイブ]                  │
│                                         │
│ ●[urgent] 引取予定時刻が近づいています   │
│   TY-2026-0140 / 30 分前                │
│   [既読化] [アーカイブ]                  │
│ ...                                    │
└─────────────────────────────────────────┘
```

### 主要要素
- フィルタタブ（未読 / すべて / アーカイブ）
- severity アイコン色分け（info/action_required/urgent）
- 一括既読 / アーカイブ操作
- カードクリックで関連依頼へジャンプ

---

## D.2 新規依頼一覧（screen-list.md §4.1）

標準テーブル。**招待経由バッジ**で通常指名と区別表示。

---

## D.3 依頼詳細（screen-list.md §4.2）

### レイアウト

```
┌─────────────────────────────────────────────────────────────┐
│ 依頼詳細 TY-2026-0142                                       │
├─────────────────────────────────────────────────────────────┤
│ 依頼番号: TY-2026-0142                                      │
│ 整備伝票: SV-2026-0089                                      │
│ 移動パターン: 片道（引取→搬入）                               │
│                                                             │
│ ─ 引取 ─                                                     │
│ 渋谷店 〒150-XXXX 東京都渋谷区...                            │
│ 希望日時: 2026-05-23 09:30                                  │
│ 担当: 田中太郎 📞 03-xxxx-xxxx                              │
│                                                             │
│ ─ 搬入 ─                                                     │
│ 横浜整備工場 〒220-XXXX 神奈川県横浜市...                    │
│ 希望日時: 2026-05-23 10:30                                  │
│                                                             │
│ ─ 車両情報 ─                                                 │
│ トヨタ アルファード 2020年  [品川 300 あ 1234]              │
│ 車台番号: ABCD123456789                                     │
│ 走行可否: ✓ 自走可  レッカー要否: -                         │
│                                                             │
│ ─ 注意事項 ─                                                 │
│ チャイルドシート搭載済み、荷物多数...                        │
│                                                             │
│ ─ 変更履歴 ─                                                 │
│ 2026-05-22 13:50 希望時刻変更（09:00 → 09:30）              │
│                                                             │
│ ─ 通知履歴 ─                                                 │
│ 2026-05-22 13:45 メール送信 ✓                              │
│                                                             │
│                          [対応不可] [対応可と回答 →]        │
└─────────────────────────────────────────────────────────────┘
```

---

## D.4 対応可否回答（screen-list.md §4.3）

### 「対応可」モーダル（v2.2、同意必須）

```
┌──────────────────────────────────────────┐
│ 対応可と回答                            ×│
├──────────────────────────────────────────┤
│ 以下の依頼を引き受けます：               │
│                                          │
│ 引取: 渋谷店 2026-05-23 09:30           │
│ 搬入: 横浜整備工場 2026-05-23 10:30     │
│                                          │
│ 引取予定日時: [05/23 09:30 ▾]           │
│ 搬入予定日時: [05/23 10:30 ▾]           │
│ 備考: [テキストエリア]                  │
│                                          │
│ ☑ 上記の内容で回送業務を引き受けます。  │
│   業務委託契約書に従い、引取・搬入・     │
│   返却の責任を負います。                 │
│                                          │
│ [キャンセル]                  [送信]   │
└──────────────────────────────────────────┘
```

- **同意チェックボックス必須**（チェックなしで送信不可）
- 送信時に承諾タイムスタンプ + IP + User-Agent を audit_logs に記録

### 「対応不可」モーダル

理由選択（プルダウン）+ 自由入力。「他業者へ再打診されます」と説明文。

---

## D.5 招待トークン受諾画面（screen-list.md §4.3.1、v2.1 / v2.2）

### 4 段階フロー

#### Step 1: 招待 URL アクセス（未認証）

```
┌──────────────────────────────────────────┐
│ 段取りくん                                │
│                                          │
│ ○○モータースから回送依頼の招待が       │
│ 届いています。                            │
│                                          │
│ ─ 依頼概要 ─                              │
│ 引取: 渋谷店 / 05-23 09:30              │
│ 搬入: 横浜整備工場 / 05-23 10:30        │
│ 車両: アルファード [品川 300 あ 1234]   │
│                                          │
│ ☑ 上記内容で対応します（同意）          │
│                                          │
│ [対応不可] [対応可 → 業者登録へ]        │
└──────────────────────────────────────────┘
```

#### Step 2: 業者情報入力（未登録業者の場合）

```
┌──────────────────────────────────────────┐
│ 業者情報の入力                            │
├──────────────────────────────────────────┤
│ 業者名: [        ]                       │
│ 担当者: [        ]                       │
│ 連絡電話: [      ]                       │
│ ログイン用メール: [        ]             │
│                                          │
│ [前へ]                      [次へ →]    │
└──────────────────────────────────────────┘
```

#### Step 3: パスワード設定（Supabase Auth 招待メール）

```
ご登録のメールアドレスにパスワード設定リンクを送信しました。
メールをご確認ください。
```

#### Step 4: 業者ポータルへ

ログイン完了 → 業者ポータルへリダイレクト。

### 競合エラー時の表示（v2.2）

```
┌──────────────────────────────────────────┐
│ 申し訳ございません                        │
│                                          │
│ この依頼は既に他の業者が受注しました。   │
│ または招待期限が切れています。           │
│                                          │
│ ご不明点は ◯◯モータース 渋谷店          │
│ 📞 03-xxxx-xxxx までご連絡ください。    │
└──────────────────────────────────────────┘
```

---

## D.6 進捗更新（screen-list.md §4.4）

依頼詳細内で進捗ステップを表示 + ボタンで遷移：

```
[引取予定 ✓] → [引取済み ✓] → [搬入済み] → [完了]
                                ↑ 現在
[搬入完了報告] [写真添付]
```

- 各ステップ確定時は タイムスタンプ + 写真添付（任意）
- 完了報告後に店舗側「差戻し」されると業者へ通知

---

# §E. 顧客予約画面（5 画面）

## E.1 予約フロー（ステップ進行型、screen-list.md §5.1）

### Step 1: 店舗選択

```
┌──────────────────────────────────────────┐
│ 段取りくん  整備予約                      │
├──────────────────────────────────────────┤
│ 整備をご希望の店舗をお選びください         │
│                                          │
│ [📍 渋谷店]      [📍 横浜店]              │
│  営業 9-18時      営業 9-18時             │
│                                          │
│ [📍 川崎店]      [📍 横須賀店]            │
└──────────────────────────────────────────┘
```

### Step 2: 作業メニュー選択（カード型）

カードクリックで標準時間 + 料金目安表示。

### Step 3: 空き日時選択

カレンダー + 時間スロット（緑=空き / グレー=満枠）

### Step 4: 顧客 / 車両情報入力

```
┌──────────────────────────────────────────┐
│ お客様情報                                │
├──────────────────────────────────────────┤
│ お名前 *: [        ]                     │
│ 電話 *:  [        ]                      │
│ メール *: [        ]                     │
│                                          │
│ ─ 車両情報 ─                              │
│ 車種:    [        ]                      │
│ ナンバー: [        ]                     │
│                                          │
│ [戻る]              [認証コードを送信]   │
└──────────────────────────────────────────┘
```

### Step 5: 認証コード入力

```
┌──────────────────────────────────────────┐
│ 認証コードを入力してください              │
│                                          │
│ ご登録のメールアドレスに 6 桁のコードを   │
│ 送信しました。                            │
│                                          │
│  [ ][ ][ ][ ][ ][ ]                     │
│                                          │
│ 受信できない場合 → [再送信]              │
└──────────────────────────────────────────┘
```

### Step 6: 完了画面

```
┌──────────────────────────────────────────┐
│ ✓ ご予約完了                              │
│                                          │
│ 予約番号: R-2026-0042                    │
│ 日時: 2026-05-23 10:00                   │
│ 店舗: 渋谷店                              │
│ 作業: オイル交換                          │
│                                          │
│ 確認メールをお送りしました。             │
└──────────────────────────────────────────┘
```

---

## E.2 予約確認（token URL、screen-list.md §5.2）

予約完了メールの URL からアクセス。予約内容表示 + [変更] [キャンセル] ボタン。

## E.3 予約変更（modify token、screen-list.md §5.3）

日時変更フォーム（空き枠から再選択） + 確定で旧トークン失効。

## E.4 キャンセル（cancel token、screen-list.md §5.4）

確認モーダル + キャンセル理由（任意）+ 確定。

## E.5 認証コード再送（screen-list.md §5.5）

レート制限あり（1 分 1 回、1 日 5 回）。

---

# §F. 設定画面（18 画面、本部管理者・店長向け）

## F.0 設定トップ

```
┌──────────────────────────────────────────────────────────────┐
│ 設定                                                          │
├──────────────────────────────────────────────────────────────┤
│ ─ 会社・店舗 ─                                                 │
│ [会社設定]  [店舗管理]  [営業時間/休日]                       │
│                                                              │
│ ─ レーン・作業 ─                                              │
│ [レーン種別]  [レーン]  [レーン稼働時間]                      │
│ [作業カテゴリ]  [作業メニュー]                                │
│                                                              │
│ ─ 予約・ステータス ─                                          │
│ [予約枠]  [ステータス]  [状態遷移ルール]                      │
│                                                              │
│ ─ 業者 ─                                                      │
│ [業者マスター]  [業者ユーザー]                                │
│                                                              │
│ ─ 通知・権限 ─                                                │
│ [通知ルール]  [権限]  [表示項目]                              │
│                                                              │
│ ─ コンプライアンス ─                                          │
│ [顧客本人確認]  [監査ログ]                                    │
└──────────────────────────────────────────────────────────────┘
```

カードグリッド型のメニュー。各カードクリックで該当設定画面へ。

## F.1～F.18 各設定画面

すべて以下の共通パターン：

- **一覧テーブル**（既存設定）+ [+ 新規追加] ボタン
- **新規追加 / 編集モーダル**（フィールドはそれぞれ `screen-list.md` §3 参照）
- **削除確認モーダル**（影響範囲を警告）
- **並び替え**（drag handle）
- **使用可否 toggle**

代表例：

### F.4 レーン設定

```
┌──────────────────────────────────────────────────────────┐
│ レーン設定                            [+ 新規レーン]      │
├──────────────────────────────────────────────────────────┤
│ ⇅ │ 店舗   │ レーン名 │ 種別     │ 同時数 │ 状態 │ 操作 │
│ ⇅ │ 渋谷店 │ Lane1   │メンテナンス│ 1     │有効  │ ⋮   │
│ ⇅ │ 渋谷店 │ Lane2   │ 重整備   │ 1     │有効  │ ⋮   │
│ ⇅ │ 横浜店 │ Lane1   │ 汎用     │ 2     │有効  │ ⋮   │
└──────────────────────────────────────────────────────────┘
```

クリックで編集モーダル：

```
┌──────────────────────────────────────────┐
│ レーン編集                              ×│
├──────────────────────────────────────────┤
│ 店舗 *:   [渋谷店 ▾]                    │
│ レーン名: [Lane1]                        │
│ 種別:    [メンテナンス ▾]                │
│ 同時予約数: [1]                          │
│                                          │
│ ─ 対応可能メニュー（M2M） ─             │
│ ☑ オイル交換                            │
│ ☑ タイヤ交換                            │
│ ☐ 車検整備                              │
│                                          │
│ ─ 稼働時間（曜日ごと） ─                 │
│ 月 [09:00-18:00]  ☐定休                │
│ 火 [09:00-18:00]  ☐定休                │
│ ...                                    │
│                                          │
│ ☑ 使用可否                              │
│                                          │
│ [削除]                  [キャンセル][保存]│
└──────────────────────────────────────────┘
```

### F.10 業者マスター

特に重要な設定。中間テーブル M2M で対応エリア / 店舗 / 曜日を編集。（管理者画面の内部フラグ、UI 非表示。設定は admin が直接 DB or system console で行う）。

### F.13 状態遷移ルール

ビジュアル状態遷移エディタ（オプション）：ノード = ステータス、矢印 = 遷移、矢印クリックで権限設定。

---

# §G. 印刷レイアウト（3 種類）

## G.1 整備伝票 PDF（screen-list.md §6.1）

- A4 縦
- ヘッダ：会社ロゴ + 段取りくん（小） + 伝票番号
- 顧客情報 + 車両情報 + 作業内容
- 担当者署名欄
- 印刷日時 + ページ番号

## G.2 回送依頼書 PDF（screen-list.md §6.2、v2.2 で強化）

- A4 縦
- ヘッダ：会社ロゴ + 業者名 + 依頼番号
- 引取 / 搬入 / 返却店舗（住所付き、Google Maps QR 埋め込み）
- 移動パターンアイコン
- 車両情報、走行可否、レッカー要否（赤太字）
- 希望日時、注意事項
- **担当者連絡先 + 緊急連絡先**
- **承諾サイン欄**（業者担当者名 / 日付 / 押印 or 電子サイン）
- フッタ：**責任分界・免責文言**（companies.transport_disclaimer_text から動的反映）

## G.3 店間移動指示書 PDF（screen-list.md §6.3）

ドライバー向けの簡易版。A4 横、車両 / 引取 / 搬入 / 連絡先のみ大きく表示。

---

# §H. レスポンシブ・モバイル対応

## H.1 ブレークポイント

| BP | 幅 | 対応 |
|---|---|---|
| sm | < 640px | 顧客予約画面 / 業者ポータルのみフル対応 |
| md | 640-1024 | 業者ポータル / 主要管理画面 |
| lg | > 1024px | 全画面 |

## H.2 モバイル時の挙動

- サイドバー → ハンバーガーメニュー
- テーブル → カード型に変換
- カレンダー → 日表示固定
- フォーム → 全幅 + ステップ進行型強調

## H.3 業者ポータルのモバイル最重要

- 業者は外出先・スマホで使用
- 進捗更新ボタンは最下部固定（thumb zone）
- 写真添付はカメラ直接起動
- オフライン時はキャッシュから直近案件表示

---

# §I. アクセシビリティ

- WCAG 2.1 AA 準拠（顧客予約画面は特に）
- すべてのインタラクティブ要素にキーボードアクセス
- フォームには aria-label / aria-describedby
- 色だけに依存しない情報伝達（ステータスバッジは色 + アイコン + テキスト）
- フォーカスリングは濃紺で明確に
- ライト / ダークモード対応（オプション、Phase 4）

---

# §J. ハンドオフバンドル要求

Claude Design から **以下の形式** でアウトプットをもらいたい：

1. **コンポーネントライブラリ**（shadcn/ui ベース、React + TypeScript + Tailwind）
2. **デザイントークン**（`tokens.json` 形式、カラー / フォント / スペーシング / 影）
3. **画面 HTML プロトタイプ**（各画面 1 ファイル、状態違いも含む）
4. **コピーライティング**（マイクロコピー、エラーメッセージ、空状態メッセージ）
5. **画像 / イラスト**（空状態用、エラー用、オンボーディング用、各画面 1-2 点）
6. **Figma ファイル**（あれば）

---

# §K. 優先度（営業資料に組み込む 13 ページ用）

営業資料に画像として組み込みたい順位：

| 優先 | 画面 | 資料ページ |
|---|---|---|
| ★★★ | C.1 ダッシュボード | p.5 |
| ★★★ | C.2 ピット予約カレンダー | p.5 |
| ★★★ | C.4 店間整備依頼（Step 3 が特に重要） | p.6 |
| ★★★ | C.5 業者通知・回送管理 | p.6, 7 |
| ★★★ | D.3 業者ポータル依頼詳細 | p.8 |
| ★★ | D.4 対応可否回答（同意モーダル含む） | p.8 |
| ★★ | D.5 招待トークン受諾画面 | p.7 (機能訴求) |
| ★★ | E.1 顧客予約フロー（Step 2-3） | p.9 |
| ★★ | C.6 整備伝票一覧 | p.6 (背景) |
| ★ | C.8 通知失敗・運用画面 | p.7 (信頼性) |
| ★ | F.0 設定トップ | p.12 (拡張性) |

これらを **画像クオリティ高め** で生成依頼。それ以外は雛形レベルで OK。

---

# §L. NG / 注意事項

- マスコットキャラクターは作らない（or 最小限のロゴマーク）
- アイコンや色だけで意味を伝えない（必ずテキスト併用）
- 個人情報（電話 / メール / VIN）はモックデータでもプレースホルダ風（`090-xxxx-xxxx`、`u***@example.com`）
- 業者ポータルは「外部利用者」前提のシンプル UX（管理画面と明確に差別化）
- 過度なアニメーション / マイクロインタラクションは避ける（BtoB 業務、軽快感は余白とリズムで出す）

---

# 補足

このハンドオフ資料の更新は Claude Code が一元管理。Claude Design 側で生成された結果は別途バンドルとして受け取り、営業資料 13 ページに組み込みます。
